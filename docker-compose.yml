version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: guardiapp
    container_name: guardiapp-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - guardiapp-network
    depends_on:
      - db
      - redis

  web:
    image: nginx:alpine
    container_name: guardiapp-nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - guardiapp-network
    depends_on:
      - app

  db:
    image: postgres:15-alpine
    container_name: guardiapp-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-guardiapp}
      POSTGRES_USER: ${DB_USERNAME:-guardiapp}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
    networks:
      - guardiapp-network

  redis:
    image: redis:alpine
    container_name: guardiapp-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - guardiapp-network

networks:
  guardiapp-network:
    driver: bridge

volumes:
  dbdata:
    driver: local
